env        = "dev"
aws_region = "eu-west-2"

# dev.impulsoiq.rinegansolutions.com. No www alias — nobody types www on a
# dev host, and it would only add a certificate name to validate.
subdomain = "dev"
www_alias = false
